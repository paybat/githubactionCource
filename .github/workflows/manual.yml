on:
  workflow_dispatch:
    inputs:
      environment:
        description: "choose enviremment"
        required: true
        default: "staging"
        type: choice
        options:
          - staging
          - production
      
    

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy application
        run: echo "deploying the application ."
