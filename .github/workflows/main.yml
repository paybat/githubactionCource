name: First WorkFlow

on :
  push :
    branches:
      - main
jobs:
  example-job :
    runs-on: ubuntu-latest

    steps:
      - name: print-welcome message
        run : ech= "welcom to our first githubAction worckFlow" 
